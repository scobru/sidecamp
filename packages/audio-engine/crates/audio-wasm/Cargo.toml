[package]
name = "audio-wasm"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
# We keep dependencies minimal for ultra-fast compilation & tiny WASM footprint
