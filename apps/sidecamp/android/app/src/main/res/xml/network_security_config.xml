<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Self-hosted server URLs are often plain HTTP on the local LAN -->
    <base-config cleartextTrafficPermitted="true">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>
</network-security-config>
